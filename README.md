# Local Finance Export Vault

Live: https://local-finance-export-vault.sociobot.in — built by the Param Factory (`pwa-offline`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
