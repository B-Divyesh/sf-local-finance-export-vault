[
  {
    "id": "demo-two-exports",
    "claim": "The demo loads two realistic budget exports.",
    "where": "demo route and README",
    "test": "npm test -- --grep @claim:demo-two-exports",
    "sandbox": "fresh browser context at /demo; assert two named sample archives"
  },
  {
    "id": "local-only",
    "claim": "Financial rows stay in this browser.",
    "where": "landing facts and privacy page",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh browser context at /demo; intercept all requests and inspect IndexedDB"
  },
  {
    "id": "license-privacy",
    "claim": "The app has no analytics or tracking request, and license verification sends only the token to api.sociobot.in.",
    "where": "privacy page and README",
    "test": "npm test -- --grep @claim:license-privacy",
    "sandbox": "fresh real vault; import a private row, intercept all requests, verify a fixture license, and assert the only external request contains no archive data"
  },
  {
    "id": "packet-contents",
    "claim": "The migration packet includes original files, standard rows, field matches, and tamper-check codes.",
    "where": "packet maker and README",
    "test": "npm test -- --grep @claim:packet-contents",
    "sandbox": "demo data; download and inspect the ZIP and manifest"
  },
  {
    "id": "hash-manifest",
    "claim": "Each archive records SHA-256 tamper-check codes for the original file and standard rows.",
    "where": "archive details and README",
    "test": "npm test -- --grep @claim:hash-manifest",
    "sandbox": "demo data; inspect two 64-character hashes in one manifest"
  },
  {
    "id": "encrypted-packet",
    "claim": "A migration packet can be encrypted and reopened with a password.",
    "where": "packet maker and README",
    "test": "npm test -- --grep @claim:encrypted-packet",
    "sandbox": "demo data; download an AES-256-GCM envelope, then reopen it to a ZIP with the password"
  },
  {
    "id": "common-imports",
    "claim": "The vault imports YNAB, Monarch, and Actual CSV files, plus files with common date and amount columns.",
    "where": "how it works and README",
    "test": "npm test -- --grep @claim:common-imports",
    "sandbox": "fresh real vault; upload and seal an Actual-shaped CSV while demo covers YNAB and Monarch"
  },
  {
    "id": "field-review",
    "claim": "You can review and change each export field before saving an archive.",
    "where": "landing archive start and README",
    "test": "npm test -- --grep @claim:field-review",
    "sandbox": "fresh real vault; upload a CSV, change its category match, seal it, and inspect the saved field matches"
  },
  {
    "id": "validation",
    "claim": "The vault validates dates and amounts and flags possible duplicate rows.",
    "where": "README and archive validation panel",
    "test": "npm test -- --grep @claim:validation",
    "sandbox": "fresh real vault; upload duplicate rows with invalid dates and inspect both validation notices"
  },
  {
    "id": "browser-persistence",
    "claim": "Sealed archives stay in IndexedDB after a reload.",
    "where": "privacy page and README",
    "test": "npm test -- --grep @claim:browser-persistence",
    "sandbox": "fresh browser context; seal one CSV, reload, and find the archive"
  },
  {
    "id": "encrypted-local",
    "claim": "A saved local archive can be encrypted with a password before it reaches IndexedDB.",
    "where": "vault review, privacy page, and README",
    "test": "npm test -- --grep @claim:encrypted-local",
    "sandbox": "fresh real vault; encrypt one archive, assert plaintext is absent from IndexedDB, reload, reject a wrong password, and reopen with the right password"
  },
  {
    "id": "demo-isolation",
    "claim": "The demo never reads or writes real vault archives.",
    "where": "demo route, demo banner, demo guide, privacy page, and README",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh context; save a named real archive, enter and leave demo twice, and assert the real archive never appears under the demo banner"
  },
  {
    "id": "scope-limits",
    "claim": "The vault does not connect to banks or change your original files.",
    "where": "landing limits and README",
    "test": "npm test -- --grep @claim:scope-limits",
    "sandbox": "fresh real vault; intercept requests while importing and downloading a packet, then assert no external request and byte-for-byte original CSV output"
  },
  {
    "id": "free-tier",
    "claim": "The free vault stores two archives; a $12 one-time license allows unlimited archives.",
    "where": "landing facts, paid section, and terms",
    "test": "npm test -- --grep @claim:free-tier",
    "sandbox": "fresh browser context; add two drafts, assert free limit, price, and Sociobot checkout route"
  },
  {
    "id": "billing-checkout",
    "claim": "The $12 purchase link opens Sociobot's hosted checkout.",
    "where": "paid ticket, terms, and README",
    "test": "npm test -- --grep @claim:billing-checkout",
    "sandbox": "fresh browser context; request the exact Sociobot checkout URL, assert its 303 redirect to the allow-listed Dodo checkout host, and assert the hosted page is reachable"
  },
  {
    "id": "offline-reload",
    "claim": "Runs offline after the first visit.",
    "where": "landing facts and README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load /demo, confirm the installed shell, disable network, and reload"
  }
]
